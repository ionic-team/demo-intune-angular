import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'demo-intune-angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
